name: Variables and Secrets Example

on:
  push:
    branches:
      - main

jobs:
  example:
    runs-on: ubuntu-latest

    steps:
      - name: Use variable
        run: echo "App environment is ${{ vars.APP_ENV }}"

      - name: Use secret
        run: echo "Using secure API key ${{ secrets.API_KEY }}"
